import { defineStore } from 'pinia';

// eslint-disable-next-line import/prefer-default-export
export const useGetProductApi = defineStore('GetProductApi', () => {
  const test = 0;
  return {
    test,
  };
});
